export default class Topic {
  constructor(id, title, description, date, user_id) {
    this.id = id;
    this.title = title;
    this.description = description;
    this.date = date;
    this.user_id = user_id;
  }
}
