export default class Authenticator {
  authenticate(user) {
    return new Promise((resolve, reject) => {
      // const user = {
      //   id: 1234,
      //   name: 'Lucas Medeiros',
      //   email: 'lucas@example.com'
      // };

      // resolve(user);

      reject();
    });
  }
}
